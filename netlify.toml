[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (safe with HashRouter; needed if you switch to BrowserRouter)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
